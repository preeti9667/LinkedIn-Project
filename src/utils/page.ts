// import { setCookie, deleteCookie } from 'js-cookie';

export const login = () => {
  // Mock login: set a token in cookies
  // setCookie('token', 'your-token-value', { maxAge: 60 * 60 * 24 }); // Expires in 1 day
};

export const logout = () => {
  // Remove the token from cookies
  // deleteCookie('token');
};
